# Slicing the Strand 🧬
# A whole genome is HUGE. Usually you only care about one little gene inside it.
# A SLICE snips out the piece you want. And [::-1] reads a strand backwards.
#
# Tip: leave each "pass" until you replace it with your code — don't just
# delete it, or Python will complain that a function is empty.


def snip(dna, start, end):
    # Give back the piece from spot `start` up to (but NOT including) `end`.
    # The trick is dna[start:end].
    pass


def flip(dna):
    # Give back the strand read backwards.
    # The trick is dna[::-1] — "step by -1", from the end to the start.
    pass
