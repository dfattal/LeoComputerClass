# Counting & Swapping Bases 🧬
# Strings come with built-in helpers that do big jobs in one move.
# .count() tallies a base. .replace() swaps one letter for another.
#
# Tip: leave each "pass" until you replace it with your code — don't just
# delete it, or Python will complain that a function is empty.


def count_base(dna, base):
    # Give back HOW MANY times `base` shows up in the strand.
    # The trick is dna.count(base).
    pass


def to_rna(dna):
    # Give back the RNA: the same strand, but every "T" swapped for a "U".
    # The trick is dna.replace("T", "U").
    pass
