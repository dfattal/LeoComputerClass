# Lists: a Backpack of Bases 🎒
# A list is a backpack: it holds many items in order, and you can add more.
# list(dna) unpacks a strand into a backpack of bases.
# backpack.append(x) drops one more item in the back.
#
# Tip: leave each "pass" until you replace it with your code — don't just
# delete it, or Python will complain that a function is empty.


def bases_list(dna):
    # Give back a LIST (backpack) of the single bases in the strand.
    # The trick is list(dna).
    pass


def add_base(genes, base):
    # Drop one more `base` into the back of the `genes` list, then return the list.
    # Use genes.append(base) — then return genes.
    pass
