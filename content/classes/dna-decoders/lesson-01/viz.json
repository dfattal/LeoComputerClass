{
  "type": "draw",
  "title": "Your DNA — the top row is the whole strand, the square below marks the base you grabbed",
  "todo": "Finish first_base() to light up the very first base of the strand!",
  "setup": "BASE_COLOR = {\"A\": \"green\", \"T\": \"red\", \"C\": \"blue\", \"G\": \"yellow\", \"U\": \"purple\"}\n\ndef __paint(dna):\n    return [BASE_COLOR.get(base, \"\") for base in dna]\n\ndef __marker(dna, spot):\n    row = [\"\" for _ in dna]\n    base = first_base(dna) if spot == 0 else last_base(dna)\n    row[spot] = BASE_COLOR.get(base, \"\")\n    return row\n\ndef __show_first(dna=\"ATCGTAGC\"):\n    return [__paint(dna), __marker(dna, 0)]\n\ndef __show_last(dna=\"ATCGTAGC\"):\n    return [__paint(dna), __marker(dna, len(dna) - 1)]\n",
  "stages": [
    {
      "fn": "__show_first",
      "args": [],
      "label": "First",
      "expected": [
        ["green", "red", "blue", "yellow", "red", "green", "yellow", "blue"],
        ["green", "", "", "", "", "", "", ""]
      ],
      "caption": "✅ You grabbed the first base! The green square sits under spot 0."
    },
    {
      "fn": "__show_last",
      "args": [],
      "label": "Last",
      "expected": [
        ["green", "red", "blue", "yellow", "red", "green", "yellow", "blue"],
        ["", "", "", "", "", "", "", "blue"]
      ],
      "caption": "🧬 First and last — you can now read any base by its spot!"
    }
  ]
}
