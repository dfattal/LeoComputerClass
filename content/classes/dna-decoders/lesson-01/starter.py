# DNA is a String of Letters 🧬
# A strand of DNA is just a string of letters, like "ATCGTAGC".
# Each letter is one BASE. You grab a base by its SPOT (its index).
#
# Tip: leave each "pass" until you replace it with your code — don't just
# delete it, or Python will complain that a function is empty.


def first_base(dna):
    # Give back the FIRST base of the strand.
    # The first spot is 0, so dna[0] is the first base.
    pass


def last_base(dna):
    # Give back the LAST base of the strand.
    # Spot -1 means "one step back from the end" — the very last base.
    pass
