# Sorting I — Bubble Sort: swap out-of-order neighbors until it's sorted.
#
# Tip: leave each "pass" until you replace it with your code.


def bubble_sort(arr):
    # Copy first: a = list(arr). Then two loops — compare a[j] with a[j+1],
    # and swap them if a[j] is bigger. Return the sorted copy.
    pass


def bubble_steps(arr):
    # Same sort, but count the swaps: swaps += 1 after each swap. Return swaps.
    pass


print("Press Run to see the swap-count hill on the Graph panel!")
