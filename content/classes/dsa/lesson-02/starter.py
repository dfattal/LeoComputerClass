# Linear Search — check every box from the front until you find it.
#
# Tip: leave each "pass" until you replace it with your code.


def linear_search(arr, target):
    # Loop over the indexes. Return i when arr[i] == target.
    # If the loop finishes with no match, return -1.
    pass


def search_steps(arr, target):
    # Count how many boxes you open. steps += 1 each time you look,
    # stop and return steps when you find the target.
    pass


print("Press Run to see your search line on the Graph panel!")
