# Sorting II — Merge Sort: split, sort the halves, merge them back.
#
# Tip: leave each "pass" until you replace it with your code.


def merge(a, b):
    # Both lists are already sorted. Use markers i and j; always copy the smaller
    # front item, then add the leftovers a[i:] and b[j:]. Return the merged list.
    pass


def merge_sort(arr):
    # Base case: a list of 0 or 1 is already sorted (return a copy).
    # Otherwise split at mid, merge_sort each half, then merge them.
    pass


def merge_steps(arr):
    # Count the work: base case 0 for len <= 1; otherwise
    # merge_steps(left) + merge_steps(right) + len(arr).
    pass


print("Press Run to watch merge sort dive under the bubble hill on the Graph!")
