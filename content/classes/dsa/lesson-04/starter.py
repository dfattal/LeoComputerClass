# Stacks & Queues — both add to the back; they differ on which end leaves.
#
# Each function returns the NEW list. Tip: leave each "pass" until you replace it.


def push(stack, x):
    # Add x to the back (top) of the stack.
    pass


def pop(stack):
    # Remove the top item (the last one). Empty stays empty.
    pass


def enqueue(queue, x):
    # Add x to the back of the line (same move as push).
    pass


def dequeue(queue):
    # Remove the FRONT item (the first one) — that's what makes it a queue.
    pass


print("Press Run to watch the cells fill and drain on the Canvas!")
