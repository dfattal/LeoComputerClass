# How Fast Is Fast? — count the steps a job takes.
#
# Each function returns a NUMBER: how many steps the job costs for a size of n.
# Tip: leave each "pass" until you replace it with your code, so Python doesn't
# complain about an empty function.


def constant_steps(n):
    # Always 1 step, no matter how big n is. (You don't even need to use n.)
    pass


def linear_steps(n):
    # One step for each item: n items -> n steps. Use a loop that runs n times.
    pass


print("Press Run to see your two lines on the Graph panel!")
