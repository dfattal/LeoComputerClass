# Binary Search — the list is SORTED, so halve it with every guess.
#
# Tip: leave each "pass" until you replace it with your code.


def binary_search(sorted_arr, target):
    # lo = 0, hi = last index. While lo <= hi: check the middle, then move
    # lo or hi to keep the half that could still hold target. Return -1 if missed.
    pass


def binary_steps(sorted_arr, target):
    # Same search, but count the middle-checks: steps += 1 at the top of the loop.
    pass


print("Press Run to see your curve race the linear line on the Graph panel!")
