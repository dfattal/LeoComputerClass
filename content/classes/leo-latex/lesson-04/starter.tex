% Page four: the self-replicating machine.

\section{The Infinite Tower}

The exponential series, built brick by brick, typeset by {{FIRST_NAME}}.

%% sum-machine
Sigma notation is just a spelled-out sum, folded up:

% Four terms (1 + x + x^2/2! + x^3/3!) = one \sum with n from 0 to 3:


%% infinite-tower
Let the tower grow forever and it becomes the most famous function there is:

% e^x = an infinite sum. The top bound is \infty :


%% self-replicating
And the reason it exists at all — it is its own derivative:

% One derivative, one beautiful fact:

