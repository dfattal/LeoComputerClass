% Page seven: the great split.

\section{The Great Split}

A rotation goes into the tower — and geometry comes out, typeset by {{FIRST_NAME}}.

%% the-split
Feed ix into the exponential series and it splits into two families:

% e^{ix} = the real family + i times the imaginary family:


%% circle-sum
Multiply the split by its conjugate — the twins trick from last week:

% Use an align* block for the two-line derivation:
% $$ \begin{align*}
% (the product) &= (what it expands to) \\
% &= (the final number)
% \end{align*} $$


%% sine-slope
Sine and cosine are now just infinite polynomials — so the power rule runs them:

% d/dx of sin(x) = ?

