% Page five: the mirror universe.

\section{The Mirror Universe}

The natural logarithm — the exponential's undo button, typeset by {{FIRST_NAME}}.

%% undo-button
Each one unwinds the other, both ways round:

% A 3-link chain: e^(ln x) = ln(e^x) = x. Grow the parens with \left( \right):


%% mirror-slope
The transcendental function whose slope is a plain fraction:

% d/dx of ln(x) — the fact you proved with the chain rule:


%% log-law
The mirror of the exponent rule — products become sums:

% ln(ab) = ... what?

