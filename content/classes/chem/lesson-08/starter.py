# Grow a Crystal 💎
# A CRYSTAL is atoms lined up into a perfect, repeating pattern. Table salt is
# sodium and chlorine atoms taking turns in a neat checkerboard. It grows
# outward in square rings (layers) from one seed atom.
# A crystal with `layers` rings is a square that's (2 * layers + 1) on a side.
#
# Tip: leave each "pass" until you replace it with your code.


def lattice_side(layers):
    # How many atoms along one side. Start at 1 (the seed); each layer adds one
    # atom on each side, so that's 2 per layer. The formula: 2 * layers + 1.
    pass


def crystal_size(layers):
    # The TOTAL number of atoms = side times side.
    # Tip: call lattice_side(layers) first, then multiply it by itself.
    pass


print("Press Run, then flip Seed -> Growing -> Full crystal to grow your gem! 💎")
