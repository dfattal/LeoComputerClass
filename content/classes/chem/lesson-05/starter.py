# Reactions = Rearranging ⚗️
# When stuff reacts, atoms don't appear or disappear — they just get RE-ARRANGED
# into new molecules. So if you count the atoms before and after, you get the
# SAME number every time. That rule is called conservation of mass.
#
# Our reaction:  2 H2 + 1 O2  ->  2 H2O   (hydrogen + oxygen makes water)
#
# Tip: leave each "pass" until you replace it with your code.


def atoms_of(num, each):
    # Total atoms = number of molecules times atoms in each molecule.
    # 2 molecules of H2 (2 atoms each) -> 4 hydrogen atoms.
    pass


def is_balanced(left, right):
    # A reaction is balanced when the count BEFORE (left) equals the count
    # AFTER (right). Return True or False.
    pass


print("Press Run, then flip Before and After — count the balls and prove they match! ⚗️")
