# Mixing Colors & Chromatography 🌈
# A black marker isn't really black — it's a SECRET mix of colored pigments!
# To un-mix them, dab a dot near the bottom of a paper strip and let water
# creep up. Each pigment is carried along at its OWN speed, so they spread into
# separate bands. Fast pigments climb high; slow ones stay low.
# The paper is 7 squares tall.
#
# Tip: leave each "pass" until you replace it with your code.

MAX = 7  # the paper is only 7 squares tall.


def travel(speed, time):
    # How far up a pigment has climbed = speed * time.
    # But it can't climb off the paper! Cap it at MAX using min(..., MAX).
    pass


def highest(speeds, time):
    # Given a list of pigment speeds, how far has the HIGHEST one climbed?
    # Tip: use max(...) with travel(s, time) for each speed s.
    pass


print("Press Run, then flip Start -> Climbing -> Separated to un-mix the colors! 🌈")
