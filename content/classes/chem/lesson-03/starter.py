# Sticking Atoms Together 🔗
# Atoms join into molecules by SHARING outer electrons. Each shared pair is a
# BOND — we'll draw it as a little stick between two atoms.
# An atom keeps bonding until its outer shell is full. Most atoms want 8 out
# there; tiny hydrogen is happy with just 2.
#
# Tip: leave each "pass" until you replace it with your code.


def bonds_needed(outer, wants):
    # Bonds = how many MORE electrons the atom needs to fill its outer shell.
    # That's just the difference: wants minus outer.
    pass


def molecule_atoms(counts):
    # counts is a list like [2, 1] (2 hydrogens + 1 oxygen = water).
    # Add them all up to get the total number of atoms.
    pass


print("Press Run to snap atoms into water, oxygen, and carbon dioxide! 🔗")
