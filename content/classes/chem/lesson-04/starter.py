# Solid, Liquid, Gas 🧊💧☁️
# The same stuff can be ice, water, OR steam — what changes is how much ENERGY
# (heat) it has. Cold particles pack together tight. Heat them up and they
# spread apart. Temperature here goes from 0 (freezing) to 100 (boiling).
#
# Tip: leave each "pass" until you replace it with your code.


def spacing(temp):
    # How big a gap to leave between particles.
    #   cold  (temp under 34) -> 0   (packed)
    #   warm  (34 to 66)      -> 1   (loose)
    #   hot   (67 and up)     -> 2   (spread out)
    # Use if / elif / else to return the right number.
    pass


def state_name(temp):
    # Turn the spacing into a word: 0 -> "solid", 1 -> "liquid", 2 -> "gas".
    # Tip: call spacing(temp) first, then decide.
    pass


print("Press Run, then flip between Cold, Warm, and Hot to melt and boil the block! 🧊💧☁️")
