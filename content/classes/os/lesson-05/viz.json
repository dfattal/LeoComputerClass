{
  "type": "draw",
  "setup": "__C = [\"red\", \"blue\", \"green\", \"orange\", \"purple\", \"pink\", \"brown\", \"gray\"]\ndef __memrow(mem):\n    return [(__C[(v - 1) % len(__C)] if v else \"\") for v in mem]\ndef __grid():\n    frag = [1, 1, 0, 2, 0, 0, 3, 3, 0, 4, 4, 0, 0, 5, 5, 0]\n    try:\n        comp = compact(frag)\n        if not isinstance(comp, list):\n            raise ValueError\n    except Exception:\n        comp = frag\n    return [__memrow(frag), __memrow(comp)]\n",
  "resultFn": "__grid",
  "demoArgs": [],
  "title": "Top row: memory full of little gaps (Swiss cheese). Bottom row: after compact() slides every block together."
}
