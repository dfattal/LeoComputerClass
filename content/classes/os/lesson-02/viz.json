{
  "type": "draw",
  "setup": "__C = [\"red\", \"blue\", \"green\", \"orange\", \"purple\", \"pink\", \"brown\", \"gray\"]\ndef __gantt(timeline, n):\n    rows = []\n    for pid in range(n):\n        rows.append([__C[pid % len(__C)] if t == pid else \"\" for t in timeline])\n    return rows\ndef __grid():\n    try:\n        tl = round_robin([6, 3, 2], 1)\n        if not isinstance(tl, list) or not tl:\n            raise ValueError\n    except Exception:\n        tl = [0, 1, 2, 0, 1, 2, 0, 1, 0, 0, 0]\n    return __gantt(tl, 3)\n",
  "resultFn": "__grid",
  "demoArgs": [],
  "title": "Round-robin: the CPU rotates between all three programs, so the tiny job isn't stuck behind the giant one."
}
