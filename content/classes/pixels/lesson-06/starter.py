# Making Choices 🚦
# if/else lets the computer DECIDE. It checks something, then picks what to do.
#   if on:
#       return [["yellow"]]
#   else:
#       return [["black"]]


def light(on):
    # If on is True, give back a yellow square.
    # Otherwise (else), give back a black square.
    pass


def tile(n):
    # If n is even, give back a white square; if odd, a black square.
    # Trick: n % 2 == 0 is True when n is even (no leftover after sharing by 2).
    pass


print("Press Run — try light(False) and tile(7) to see the choices! 🚦")
