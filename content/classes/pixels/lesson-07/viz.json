{
  "type": "draw",
  "resultFn": "__draw",
  "demoArgs": [],
  "title": "Loops at work — press Run to paint it!",
  "setup": "# Show the checkerboard once it's done, otherwise the single looped row.\ndef __draw():\n    try:\n        __g = checkerboard(8)\n        if __g:\n            return __g\n    except Exception:\n        pass\n    try:\n        __g = row_of(\"blue\", 6)\n        if __g:\n            return __g\n    except Exception:\n        pass\n    return [[\"\"]]\n"
}
