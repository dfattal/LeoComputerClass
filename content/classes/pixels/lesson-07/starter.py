# Do It Again 🔁
# A loop repeats work for you. A loop INSIDE a loop fills a whole grid.
#   for i in range(n):     <- do this n times
#       row.append(color)  <- add one square each time


def row_of(color, n):
    # Build one row by appending a square n times with a loop, then return it
    # as a grid (one row inside a list).
    row = []
    # for i in range(n):
    #     row.append(color)
    # return [row]
    pass


def checkerboard(n):
    # Build an n-by-n checkerboard.
    # Outer loop walks the rows (r). Inner loop walks the squares (c).
    # For each square, DECIDE the color from (r + c): even -> black, odd -> white.
    pass
