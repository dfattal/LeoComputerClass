# Spells with Inputs 🎚️
# A parameter is a blank in your recipe that the caller fills in.
#   def bar(color, n):   <- color and n are blanks
#   bar("red", 3)        <- fills color="red", n=3


def bar(color, n):
    # Give back one row of n squares, all the given color.
    # (Like Lesson 3's bar, but now YOU pick the color too.)
    pass


def dot(color):
    # Give back a 3x3 picture with one square of the given color in the MIDDLE,
    # and empty squares "" all around it.
    pass


print("Press Run — then call dot() with a different color! 🎚️")
