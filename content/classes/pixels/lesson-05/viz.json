{
  "type": "draw",
  "resultFn": "__draw",
  "demoArgs": [],
  "title": "Your spell — press Run to paint it!",
  "setup": "# Show whichever spell the student has finished: the dot if it's done,\n# otherwise the colored bar.\ndef __draw():\n    try:\n        __g = dot(\"purple\")\n        if __g:\n            return __g\n    except Exception:\n        pass\n    try:\n        __g = bar(\"purple\", 5)\n        if __g:\n            return __g\n    except Exception:\n        pass\n    return [[\"\"]]\n"
}
