# Boxes That Remember 📦
# A variable is a labeled box. You put a value in it with =, then use its name.
#   c = "purple"      <- put the word "purple" in a box called c
#   return [[c, c]]   <- use the box wherever you want that color


def my_square():
    # Make a box called c holding "purple".
    # Then return a 2x2 square that uses c for all four squares.
    # Try it: once it works, change "purple" to "green" and Run again!
    pass


def two_tone():
    # Make TWO boxes: a = "black" and b = "yellow".
    # Return one row that goes a, b, a, b.
    pass
