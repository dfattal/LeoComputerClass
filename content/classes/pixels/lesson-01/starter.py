# Hello, Pixels! 🎨
# A picture is a GRID: a list of rows. Each row is a list of colored squares.
# Your job: fill in what each function gives BACK with the word "return".
#
# Tip: leave each "pass" until you replace it with your code — don't just
# delete it, or Python will complain that the function is empty.


def hello_pixel():
    # Give back ONE red square. A grid with one row, and that row has one cell.
    # Like this:  return [["red"]]
    pass


def rainbow():
    # Give back ONE row with six colored squares, in this order:
    # red, orange, yellow, green, blue, purple
    pass
